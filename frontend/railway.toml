# Deploy frontend — set Railway service Root Directory to: frontend
[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
numReplicas = 1
